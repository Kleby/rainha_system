model Pessoa {
    id        Int        @id @default(autoincrement())
    nome      String
    email     String     @unique
    tel       String
    enderecos EnderecoPessoa[]
    cliente   Cliente?
    usuario   Usuario?
}
