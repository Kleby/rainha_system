model Pessoa {
    id        Int        @id @default(autoincrement())
    nome      String
    email     String     @unique
    tel       String
    createdAt DateTime   @default(now())
    updatedAt DateTime   @updatedAt
    enderecos Endereco[]
    cliente   Cliente?
    usuario   Usuario?
}
